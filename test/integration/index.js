before(function () {
  // connect to db

  return Promise.resolve();
});

beforeEach(function () {
  // clean db

  return Promise.resolve();
});

after(function () {
  // disconnect from db

  return Promise.resolve();
});
